const layout = {
  header: {
    height: "150px",
  },
  main: {
    "max-width": "1500px",
  },
  footer: {
    height: "240px",
  },
};

const config = {
  layout,
};

export { layout };

export default config;

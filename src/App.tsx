import React from "react";

import Router from "@globals/Router";
import GlobalWrapper from "@globals/GlobalWrapper";

const App = () => {
  return (
    <GlobalWrapper>
      <Router />
    </GlobalWrapper>
  );
};

export default App;
